import next from "next";
import NavBar from "./navbar";

export default function Header() {
    return (
        <header>

            <div>LOGO</div>

            <NavBar/>

        </header>
    );
};